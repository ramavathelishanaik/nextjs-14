'use client';
/**
 * Renders a loading indicator for the authentication flow.
 * This component is used when the application is waiting for authentication-related data to load.
 */
export default function AuthLoading() {
  return <p className='text-center'>Loading...</p>;
}
